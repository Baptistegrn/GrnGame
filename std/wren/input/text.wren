foreign class InputText {
    foreign static get()
    foreign static clear()

    foreign static drop_file()

    foreign static put_text(text)

    foreign static remove_text_int(size)
    foreign static remove_text_char(pattern)

    foreign static replace(pattern, replace)
}