name: Build

on:
  - push
  - pull_request

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-latest, windows-latest]
        buildtype: [debug, release]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: latest
          actions-cache-folder: '.xmake-cache-v2'
          package-cache: true
          build-cache: true

      - name: Remove homebrew freetype
        if: runner.os == 'macOS'
        run: brew uninstall --ignore-dependencies freetype

      - name: Clean packages
        run: xmake require --clean -y

      - name: Configure
        run: xmake config -m ${{ matrix.buildtype }} -y -vv 
      
      - name: embedded
        run: xmake f --embed_assets=y

      - name: Build
        run: xmake build -y -vv

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.buildtype }}
          path: build/*/*/${{ matrix.buildtype }}/
